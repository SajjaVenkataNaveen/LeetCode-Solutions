class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int na = nums.size();
        if(na==1){
            return nums[0];
        }
        int ans=0;
        for(int i=0;i<na;i++){
            ans =  ans^nums[i];
        }

        return ans;

    }
};
